@model IEnumerable<LearningHub.Models.MesSendTbl>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MessageText)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ParentTbl.ParentName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MsgTransTbl.ErrCode)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MessageText)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ParentTbl.ParentName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MsgTransTbl.ErrCode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MsgSendID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MsgSendID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MsgSendID })
        </td>
    </tr>
}

</table>
